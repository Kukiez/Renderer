add_library(Mesh)

file(GLOB_RECURSE MESH_SOURCES
    CONFIGURE_DEPENDS
    *.cpp *.h
)

target_sources(my_lib PUBLIC ${MESH_SOURCES})

target_include_directories(Mesh
    PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}
)
